spring:
  application:
    name: Sistema7mares

  datasource:
    url: jdbc:postgresql://localhost:5432/seven_seas_db
    username: clau
    password:

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true